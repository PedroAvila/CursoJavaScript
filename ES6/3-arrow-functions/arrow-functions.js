//let f = (nombre, apellido) => `Hola ${ nombre } ${ apellido }`;
let f = nombre => `Hola ${ nombre }`;

console.log(f("Enmanuel"));

/* let objeto = {
    nombre: "Enmanuel",
    estado: function(){
        setTimeout(() => {
            console.log(`${ this.nombre } esta programando`);
        });
    }
};

console.log(objeto.estado()); */